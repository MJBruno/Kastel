println("TEST ARRAY LENGTH");

let values = [10, 20, 30];

println(values.length);

values.push(40);

println(values.length);

values.pop();

println(values.length);

values.clear();

println(values.length);